# Subset of requirements.txt actually needed to run src/training/train_qlora.py.
# Deliberately excludes scikit-learn, scipy, matplotlib, seaborn -- those are
# only used by the eval/data-prep scripts, and pulling them in here triggers
# a from-source numpy/scipy build that fails on SeaWulf's old system GCC
# (4.8.5, needs >=9.3). See docs/build-log.md, 2026-08-18 entry.
pandas>=2.2
datasets>=2.19
transformers>=4.42
accelerate>=0.30
tqdm>=4.66
peft>=0.11
# >=0.43 has no wheel compatible with this node's old glibc (pip caps at
# 0.42.0 -- confirmed live, not guessed); 0.42.0 still has full 4-bit NF4
# quantization support, which is all this project needs.
bitsandbytes>=0.39,<0.43
trl>=0.9
# mlflow deliberately excluded: it pulls in scipy, which hits the same
# from-source build failure as scikit-learn (GCC too old). Not needed
# here anyway -- both sbatch scripts run with --report-to none, and
# train_qlora.py never imports mlflow directly.
